# Database Configuration
# ============================================
# YLSTACK Environment Configuration
# ============================================
# Copy this file to .env and configure for your environment
#
# Supported Database Types:
# - sqlite: Local SQLite (development)
# - turso: Turso/libSQL (edge-compatible)
# - d1: Cloudflare D1 (edge-compatible)
# - postgres: PostgreSQL via Neon HTTP (edge-compatible)
# ============================================

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# Choose ONE database type and configure accordingly

# Option 1: SQLite (Default - Local Development)
# -----------------------------------------------
DATABASE_TYPE=sqlite
DATABASE_URL=sqlite.db
# Or use in-memory database:
# DATABASE_URL=:memory:

# Option 2: Turso (libSQL - Edge Compatible)
# -----------------------------------------------
# DATABASE_TYPE=turso
# DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-turso-auth-token

# Option 3: Cloudflare D1 (Edge Compatible)
# -----------------------------------------------
# DATABASE_TYPE=d1
# D1_DATABASE_ID=your-d1-database-id
# Note: D1 binding is configured in wrangler.toml

# Option 4: PostgreSQL via Neon HTTP (Edge Compatible)
# -----------------------------------------------# DATABASE_TYPE=postgres
# DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require
# Or use Neon HTTP endpoint:
# DATABASE_URL=https://your-project.neon.tech/sql

# --------------------------------------------
# Server Configuration
# --------------------------------------------
PORT=5000
NODE_ENV=development

# --------------------------------------------
# Security
# --------------------------------------------
# IMPORTANT: Change this in production!
SESSION_SECRET=your-secret-key-here-change-in-production

# --------------------------------------------
# Runtime Adapter (Optional)
# --------------------------------------------
# Specify the runtime adapter to use
# Options: cloudflare, deno, vercel, node
# RUNTIME_ADAPTER=node

# --------------------------------------------
# Cloudflare Workers Configuration (if using D1)
# --------------------------------------------
# CLOUDFLARE_ACCOUNT_ID=your-account-id
# CLOUDFLARE_API_TOKEN=your-api-token

# --------------------------------------------
# Development Tools
# --------------------------------------------
# Enable debug logging
# DEBUG=ylstack:*

# Drizzle Studio port (default: 4983)
# DRIZZLE_STUDIO_PORT=4983
